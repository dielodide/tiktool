{
  "name": "tiktool-backend",
  "version": "1.0.0",
  "description": "TikTool Anonymous Viewer - Backend API",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "fastify": "^4.28.1",
    "@fastify/cors": "^8.5.0",
    "@fastify/rate-limit": "^8.1.1",
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "https-proxy-agent": "^7.0.5",
    "ioredis": "^5.4.1",
    "pino": "^8.21.0",
    "pino-pretty": "^11.2.1"
  },
  "devDependencies": {
    "typescript": "^5.5.2",
    "ts-node-dev": "^2.0.0",
    "@types/node": "^20.14.9",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
