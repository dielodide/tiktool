{
  "name": "tiktool-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only --esm src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^8",
    "@fastify/rate-limit": "^8",
    "axios": "^1",
    "fastify": "^4",
    "ioredis": "^5",
    "pino": "^8",
    "puppeteer": "^22"
  },
  "devDependencies": {
    "@types/node": "^20",
    "ts-node-dev": "^2",
    "typescript": "^5",
    "vitest": "^1"
  }
}
